# Project Tracker — Frontend

React + Vite frontend for the project tracker Kanban app.

## Running

```bash
npm install
npm run dev
```

Runs on http://localhost:5173. Requires the backend server to be running on port 3001.

## Build

```bash
npm run build
```
